@model MvcApplication2.Models.CourseManagements

@{
    ViewBag.Title = "Create";
    var classes = (List<MvcApplication2.Models.Classes>)ViewBag.Classes;
    var teachers = (List<MvcApplication2.Models.Teachers>)ViewBag.Teachers;
    var course = (List<MvcApplication2.Models.Course>)ViewBag.Course;
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>CourseManagements</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.ClassId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ClassId)
            @Html.ValidationMessageFor(model => model.ClassId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.CourseId)
        </div>
        <div class="editor-field">
           
             <select name ="ClassId" class="editor-field">
              @foreach (var teacher in teachers)
              { 
               <option value ="@teacher.Id">@teacher.name</option>
              }
         </select>
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.TeacherId)
        </div>
        <div class="editor-field">
          
            <select name ="ClassId" class="editor-field">
              @foreach (var teacher in teachers)
              { 
               <option value ="@teacher.Id">@teacher.name</option>
              }
         </select>
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
